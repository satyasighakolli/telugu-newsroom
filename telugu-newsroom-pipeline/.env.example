# Executables receive JSON on stdin and return JSON on stdout.
# See PROVIDER_CONTRACTS.md. Keep API keys inside the adapter environment.
MEDIAOPS_SPEECH_COMMAND=python /adapters/deepgram_speech.py
# Optional. Leave blank to use the conservative local editorial fallback.
MEDIAOPS_EDITORIAL_COMMAND=
DEEPGRAM_API_KEY=replace_me
DEEPGRAM_MODEL=nova-3
DEEPGRAM_DIARIZE_MODEL=latest
